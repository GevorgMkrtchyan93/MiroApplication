<Page x:Class="Miro.Client.Views.BoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!--<Canvas x:Name="DrawingCanvas" Grid.Row="1" Background="White">-->
            <!--MouseLeftButtonDown="{Binding  DrawingCanvas_MouseLeftButtonDown, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                MouseLeftButtonUp="{Binding DrawingCanvas_MouseLeftButtonUp,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                MouseMove="{Binding DrawingCanvas_MouseMove, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>-->
    </Grid>
</Page>
